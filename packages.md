# when trying to pip install and getting errors check if the package is an application package
# check the pip online website for info and compabilities
https://pypi.org/


# i installed pip install python-decouple from --- https://pypi.org/project/python-decouple/

# NOTE: pip install python-decouple & pip uninstall decouple
# --the above will fix decouple issue
# ALLOWED_HOSTS=localhost,127.0.0.1
